<section class="blog-area ptb-140 bg-1 recent-blog">
    <div class="container">
        <div class="row">
          <div class="col-md-12 text-center">
            <h2>{{view.getTitle()}}</h2>
          </div>
          <div class="latest-blog owl-carousel owl-theme">
            {% for row in rows %}
                {{ row.content }}
            {% endfor %}
          </div>
        </div>
    </div>
</section>
